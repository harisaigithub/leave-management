import AppLayout from "../../components/AppLayout";

export default function LeaveHistory() {
  return (
    <AppLayout title="Leave history" subtitle="Search and filter your past requests.">
      <div className="card p-6 text-sm text-muted">Leave history table — built in Step 6.</div>
    </AppLayout>
  );
}
