import AppLayout from "../../components/AppLayout";

export default function ApplyLeave() {
  return (
    <AppLayout title="Apply for leave" subtitle="Submit a new leave request.">
      <div className="card p-6 text-sm text-muted">Apply leave form — built in Step 6.</div>
    </AppLayout>
  );
}
