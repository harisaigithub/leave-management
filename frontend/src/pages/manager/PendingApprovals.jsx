import AppLayout from "../../components/AppLayout";

export default function PendingApprovals() {
  return (
    <AppLayout title="Pending approvals" subtitle="Review and act on leave requests.">
      <div className="card p-6 text-sm text-muted">Pending approvals table — built in Step 7.</div>
    </AppLayout>
  );
}
