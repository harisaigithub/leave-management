import AppLayout from "../../components/AppLayout";

export default function Employees() {
  return (
    <AppLayout title="Employees" subtitle="Search employees and view their leave history.">
      <div className="card p-6 text-sm text-muted">Employee directory — built in Step 7.</div>
    </AppLayout>
  );
}
