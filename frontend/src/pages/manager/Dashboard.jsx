import AppLayout from "../../components/AppLayout";

// Full implementation (org stats + recent activity) lands in Step 7.
export default function ManagerDashboard() {
  return (
    <AppLayout title="Dashboard" subtitle="Team leave overview.">
      <div className="card p-6 text-sm text-muted">Manager dashboard — built in Step 7.</div>
    </AppLayout>
  );
}
